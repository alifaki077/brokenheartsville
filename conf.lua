function love.conf(t)
    t.title = "BROKENHEARTSVILLE"

    if t.window then
        t.window.width = 1000
        t.window.height = 400
    end
    
    if t.screen then
        t.screen.width = 1000
        t.screen.height = 400
    end

	t.identity = "brokenheartsville"
end
